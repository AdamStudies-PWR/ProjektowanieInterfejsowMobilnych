import {
  StyleSheet
} from 'react-native';

const styles = StyleSheet.create(
{
  container:
  {
      flex: 1,
      backgroundColor: '#00425C' // Set your own custom Color
  }
});

export default styles